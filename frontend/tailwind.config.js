/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
        "./src/**/*.{js,ts,jsx,tsx,mdx}", // 自分のフォルダ内を見るように設定
    ],
    theme: {
        extend: {},
    },
    plugins: [],
}