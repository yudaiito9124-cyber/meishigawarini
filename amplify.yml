version: 1
applications:
  - appRoot: frontend
    frontend:
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - npm run build
      # artifacts は削除して Amplify の自動検知に任せる
      cache:
        paths:
          - node_modules/**/*
