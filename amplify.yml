version: 1
applications:
  - appRoot: frontend
    frontend:
      phases:
        preBuild:
          commands:
            - nvm use 22 # Node.js v22を使用
            - npm ci
            - printenv | grep -e NEXT_PUBLIC_ > .env
        build:
          commands:
            - npm run build -- --no-turbo
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
