#!/bin/bash

# スクリプトの親ディレクトリに移動
cd "$(dirname "$0")"

echo "=========================================="
echo "  名刺代わりに: 開発プレビューサーバー起動"
echo "=========================================="

# 1. バックグラウンドで3秒後にブラウザでプレビューページを開く
(sleep 3 && open "http://localhost:3000/ja/help") &

# 2. フロントエンドディレクトリに移動し、サーバーを起動
echo "依存パッケージをチェック中..."
cd frontend
npm install

echo "開発サーバーを起動しています..."
npm run dev
